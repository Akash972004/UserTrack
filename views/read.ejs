<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="http://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <a class="text-1xl tracking-tighter text-blue-500 mb-3" href="/">← Back to Home</a>
        <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            
            <% if (Array.isArray(users) && users.length > 0) { %>
                <% users.forEach(function(user) { %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-22 rounded-lg bg-zinc-700">
                            <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="User Image">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-3">
                            <a class="text-blue-200" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-600" href="/delete/<%= user._id %>">Delete User</a>   
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h3>No Users Yet</h3>
            <% } %>

        </div>
    </div>
</body>
</html>
